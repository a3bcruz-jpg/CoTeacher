import type { CurriculumItem } from "./types";

/**
 * Authoritative curriculum records are intentionally empty until verified source
 * material has been imported and reviewed. Never populate this file from AI output.
 */
export const VERIFIED_CURRICULUM: CurriculumItem[] = [];
